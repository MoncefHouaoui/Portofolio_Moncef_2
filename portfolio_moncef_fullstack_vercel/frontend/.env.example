# En local, tu peux laisser cette variable vide parce que Vite utilise le proxy vers le backend.
# Pour Vercel, ne mets pas localhost : les appels API doivent rester relatifs avec /api.
# VITE_API_URL=
